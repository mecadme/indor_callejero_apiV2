# Copiar a .env (que está en .gitignore) y completar con valores reales.
# Este archivo SÍ se commitea: documenta qué variables existen, nunca sus valores.
DB_NAME=indor_callejero_dev
DB_PASSWORD=changeme_local_only

# HMAC256 -- generar uno propio por entorno, nunca reusar el de dev en prod.
# Ej: openssl rand -hex 32
JWT_SECRET=changeme_generate_your_own_random_secret
